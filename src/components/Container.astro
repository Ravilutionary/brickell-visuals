---
interface Props {
    class?: string;
    padding?: boolean;
}
const { class: className, padding = false } = Astro.props;
---
<div class={`container mx-auto px-6 2xl:px-12 max-w-[1600px] ${padding ? 'py-24 2xl:py-32' : ''} ${className || ''}`}>
    <slot />
</div>
